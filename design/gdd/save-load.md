# 存档/加载系统 (Save/Load System)

> **Status**: Designed (pending review)
> **Author**: [user + agents]
> **Last Updated**: 2026-04-23
> **Implements Pillar**: 节奏是轻松的

## Overview

存档/加载系统是《地下排练室》的**进度持久化基础设施**，负责将游戏世界中所有可变玩家进度序列化到设备本地存储，并在游戏启动时完整恢复。它是放置游戏"随时来随时走"体验的技术基石，也是"节奏是轻松的"支柱的核心保障——玩家永远不会因为设备没电、应用崩溃或误操作而丢失进度。

**系统边界**：
- **包含**：存档数据结构定义、序列化/反序列化逻辑、版本兼容处理、自动/手动存档触发、存档异常恢复、多存档槽位管理
- **不包含**：云同步实现（MVP 仅预留接口）、跨设备存档迁移、存档加密（MVP 不实现）、具体的 UI 界面（属于设置菜单UI）

**玩家如何感知**：
- 玩家**不直接感知**存档系统——它像电力一样，存在时你从不思考它
- 当玩家关闭游戏、卸载重装后发现进度还在 → "这个世界记得我"
- 当存档损坏后系统优雅恢复 → "幸好没丢太多"

**为什么必须存在**：
没有存档系统，放置游戏的核心假设"设置后离开，回来收获"就彻底崩塌。玩家不会在手机里保留一款"每次关闭都清零"的游戏。

---

## Player Fantasy

存档系统没有直接的玩家幻想——它是**基础设施**，玩家感受到的是它保障的东西：

> 你地铁上玩了两站路，设置好排练计划，然后锁屏去工作。六小时后你在晚饭前打开游戏，一切还在——林野的吉他技能涨了，大伟的心情从低落恢复到了正常，你离线时触发的那个争执事件还在等你处理。

**核心情感**：**"安全感"**。这不是激动或兴奋，而是一种底层信任——我知道我的选择不会被辜负，我知道这个世界会记住我做过什么。

**参考体验**：
- 《动物森友会》的自动存档——你从不思考它，但你知道它一直在
- 《星露谷物语》的睡存档——一天的结束就是存档的时刻，有仪式感也有安全感

**这不是"收集成就"的幻想**——这是"我的时间投入被尊重"的幻想。

---

## Detailed Design

### Core Rules

#### 1. 存档数据分层

存档数据分为三层，每层有不同的持久化策略：

| 层级 | 内容 | 持久化策略 | 大小预估 |
|------|------|-----------|----------|
| **L1: 核心进度** | 角色状态、曲目熟练度、内容解锁、事件状态、时间戳、排练计划 | **必须保存**，丢失=游戏重置 | ~10-50KB |
| **L2: 配置偏好** | 音量设置、通知偏好、UI 选项 | 丢失= inconvenience，不致命 | ~1-5KB |
| **L3: 缓存数据** | 资源加载缓存、预计算数据、临时状态 | 不保存，启动时重建 | N/A |

**MVP 只实现 L1 和 L2**。L3 在 Performance 阶段考虑。

#### 2. 存档数据结构 (Save Data Schema)

```json
{
  "save_format_version": 1,
  "save_timestamp": 1713800000,
  "game_version": "0.1.0",
  
  "l1_core_progress": {
    "characters": {
      "linye": {
        "skills": {"guitar": 42, "bass": 10},
        "mood": 65,
        "energy": 80,
        "morale": 70
      },
      "dawei": {
        "skills": {"drums": 25, "vocals": 15},
        "mood": 55,
        "energy": 60,
        "morale": 65
      }
    },
    "song_proficiency": {
      "basement_light": 35,
      "rust": 20,
      "404": 0,
      "noise_prayer": 0,
      "last_train": 0
    },
    "content_unlocks": {
      "venue_campus_stage": true,
      "combo_stubborn_rhythm": false,
      "chapter_basement_light": true,
      "chapter_first_show": false
    },
    "active_events": [
      {
        "instance_id": "evt_001",
        "template_id": "rel_argument_rehearsal",
        "status": "active_unread",
        "created_at": 1713790000,
        "variables": {"actor": "linye", "target": "dawei"},
        "selected_branch": null
      }
    ],
    "event_history": [
      {
        "instance_id": "evt_000",
        "template_id": "ms_first_show_invite",
        "resolved_at": 1713780000,
        "selected_branch": "branch_accept"
      }
    ],
    "rehearsal_plan": {
      "song_id": "rust",
      "members": ["linye", "dawei"],
      "focus": "skill",
      "set_at": 1713795000
    },
    "time_state": {
      "last_leave_timestamp": 1713798000,
      "total_play_time_seconds": 3600
    },
    "reputation": 15,
    "relationships": {
      "linye_dawei": 55
    },
    "global_flags": {
      "tutorial_completed": true,
      "first_rehearsal_done": true
    }
  },
  
  "l2_preferences": {
    "audio": {
      "master_volume": 0.8,
      "bgm_volume": 0.7,
      "sfx_volume": 1.0
    },
    "notifications": {
      "enabled": true,
      "offline_cap_reminder": true
    },
    "ui": {
      "text_size": "normal",
      "animation_enabled": true
    }
  }
}
```

#### 3. 存档触发机制

**自动存档触发点**：

| 触发时机 | 优先级 | 说明 |
|----------|--------|------|
| 游戏进入后台 | **高** | 应用 `NOTIFICATION_WM_GOES_BACKGROUND` 时立即保存 |
| 玩家完成关键操作 | **高** | 设置排练计划、处理事件、完成演出后 |
| 定时自动存档 | **中** | 每 5 分钟一次（仅当玩家有操作时） |
| 玩家退出游戏 | **高** | 点击退出按钮时 |

**手动存档**：
- 通过设置菜单中的"立即保存"按钮触发
- MVP 不实现"选择存档槽"功能（只有一个自动存档 + 一个手动备份）

**存档频率保护**：
- 两次自动存档之间最少间隔 30 秒（防止高频操作导致频繁写入）
- 如果 30 秒内多次触发，只保存最后一次的状态

#### 4. 加载流程

```
游戏启动
    │
    ▼
检查存档文件是否存在
    │
    ├── 否 → 创建新游戏（使用默认初始数据）
    │
    └── 是 → 读取存档文件
              │
              ▼
        校验 save_format_version
              │
              ├── 不匹配 → 尝试版本迁移
              │            ├── 成功 → 继续加载
              │            └── 失败 → 报错，提供"新建游戏"或"尝试修复"
              │
              └── 匹配 → 反序列化数据
                            │
                            ▼
                     校验数据完整性
                            │
                            ├── 失败 → 尝试从备份恢复
                            │            ├── 成功 → 加载备份，提示"从备份恢复"
                            │            └── 失败 → 报错，提供"新建游戏"
                            │
                            └── 成功 → 恢复游戏状态
                                          │
                                          ▼
                                   计算离线收益（时间管理/离线收益系统）
                                          │
                                          ▼
                                   显示"欢迎回来"弹窗
```

#### 5. 版本兼容策略

| 场景 | 处理方式 |
|------|----------|
| 存档版本 = 当前版本 | 正常加载 |
| 存档版本 < 当前版本（旧存档，新版本游戏） | 执行版本迁移脚本 |
| 存档版本 > 当前版本（新存档，旧版本游戏） | 拒绝加载，提示"请更新游戏" |
| 无法识别的版本号 | 视为损坏，尝试备份恢复 |

**版本迁移规则**：
- 每个版本变更只定义"从 N 到 N+1"的迁移脚本
- 跨多版本的迁移通过链式执行：N → N+1 → N+2 → current
- 迁移脚本只做**数据结构调整**，不做**数值平衡调整**（避免旧存档被 nerfed）

**MVP 版本迁移示例**：
```gdscript
# 从版本 1 迁移到版本 2（假设 v2 添加了 "chemistry" 字段）
func migrate_v1_to_v2(data: Dictionary) -> Dictionary:
    if not data.l1_core_progress.has("chemistry"):
        data.l1_core_progress.chemistry = {}
    data.save_format_version = 2
    return data
```

#### 6. 备份机制

**自动备份策略**：
- 每次保存时，先将当前存档复制为 `.backup`
- 最多保留 3 个历史备份（滚动覆盖）
- 备份文件命名：`save.json`、`save.json.backup1`、`save.json.backup2`、`save.json.backup3`

**备份恢复触发**：
- 主存档损坏（JSON 解析失败）
- 主存档数据完整性校验失败
- 版本迁移失败

#### 7. 存档完整性校验

**校验方式**：

| 校验项 | 方法 | 失败处理 |
|--------|------|----------|
| JSON 格式有效性 | `JSON.parse()` 结果检查 | 尝试备份恢复 |
| 必填字段存在 | 检查所有 L1 顶层字段 | 缺失字段用默认值填充，记录警告 |
| 数值范围合法性 | 检查状态值是否在 0-100 | 非法值硬封顶/保底，记录警告 |
| 时间戳合理性 | `last_leave_timestamp` ≤ 当前时间 | 异常时间戳 → 使用当前时间，标记异常 |
| 引用一致性 | 检查事件引用的模板ID是否存在 | 引用不存在 → 移除该事件，记录警告 |

**校验不阻塞加载**：任何校验失败都不阻止游戏启动，而是：
1. 用默认值/备份修复问题
2. 记录详细警告日志
3. UI 显示"检测到存档问题，已自动修复"（非阻断性提示）

#### 8. MVP 存档槽位设计

| 槽位 | 用途 | 自动/手动 |
|------|------|-----------|
| 槽位 0（主存档） | 当前游戏进度 | 自动保存 |
| 槽位 1（备份存档） | 手动备份点 | 手动触发 |

**MVP 不实现**：
- 多角色/多周目存档
- 云存档同步
- 存档导出/导入
- 存档加密

**Post-MVP 扩展**：
- 云存档接口（iCloud / Google Play Games）
- 最多 3 个手动存档槽
- 存档加密（防止作弊修改）

### States and Transitions

存档系统本身是一个服务，没有复杂的运行时状态。它的操作有离散状态：

| 操作状态 | 触发条件 | 玩家可见 |
|----------|----------|----------|
| **空闲 (Idle)** | 无存档操作进行中 | 无 |
| **保存中 (Saving)** | 触发自动/手动存档 | 右上角微型的保存图标旋转（0.5-1秒） |
| **加载中 (Loading)** | 游戏启动或切换存档 | 加载界面进度条 |
| **校验中 (Validating)** | 加载后数据校验 | 加载界面显示"校验数据中..." |
| **恢复中 (Recovering)** | 主存档损坏，尝试备份 | 加载界面显示"尝试恢复..." |
| **错误 (Error)** | 所有恢复尝试失败 | 显示错误弹窗，提供"新建游戏"选项 |

```
┌─────────┐    触发保存      ┌─────────┐    保存完成      ┌─────────┐
│  空闲    │ ───────────────► │ 保存中   │ ──────────────► │  空闲    │
└─────────┘                  └─────────┘                 └─────────┘
     │
     │ 游戏启动
     ▼
┌─────────┐    读取完成      ┌─────────┐    校验通过      ┌─────────┐
│ 加载中   │ ───────────────► │ 校验中   │ ──────────────► │  空闲    │
└─────────┘                  └─────────┘                 └─────────┘
     │                            │
     │ 读取失败                    │ 校验失败
     ▼                            ▼
┌─────────┐                  ┌─────────┐
│ 恢复中   │                  │ 恢复中   │
└─────────┘                  └─────────┘
     │                            │
     │ 恢复成功                    │ 恢复失败
     ▼                            ▼
┌─────────┐                  ┌─────────┐
│  空闲    │                  │  错误    │
└─────────┘                  └─────────┘
                                    │
                                    │ 玩家选择新建
                                    ▼
                              ┌─────────┐
                              │  空闲    │
                              └─────────┘
```

### Interactions with Other Systems

**输入（本系统接收的数据）**：

| 来源系统 | 输入数据 | 作用 |
|----------|----------|------|
| 角色属性系统 | 角色完整状态（技能、状态值） | 序列化保存 |
| 曲目数据库 | 曲目熟练度 | 序列化保存 |
| 内容数据库 | 内容解锁状态 | 序列化保存 |
| 事件模板系统 | 活跃/已处理事件实例 | 序列化保存 |
| 时间管理/离线收益 | `last_leave_timestamp`、排练计划 | 序列化保存 |
| 关系矩阵系统 | 角色间关系值 | 序列化保存 |
| 声誉系统 | 当前声誉值 | 序列化保存 |
| 进度解锁系统 | 全局标志、解锁进度 | 序列化保存 |
| 设置菜单UI | 玩家偏好设置 | 序列化保存（L2） |
| 音频系统 | 音量设置 | 序列化保存（L2） |

**输出（本系统提供的数据）**：

| 目标系统 | 输出数据 | 作用 |
|----------|----------|------|
| 角色属性系统 | 反序列化后的角色状态 | 恢复游戏状态 |
| 曲目数据库 | 反序列化后的熟练度 | 恢复游戏状态 |
| 内容数据库 | 反序列化后的解锁状态 | 恢复游戏状态 |
| 事件模板系统 | 反序列化后的事件实例 | 恢复游戏状态 |
| 时间管理/离线收益 | `last_leave_timestamp` | 计算离线收益 |
| 关系矩阵系统 | 反序列化后的关系值 | 恢复游戏状态 |
| 声誉系统 | 反序列化后的声誉值 | 恢复游戏状态 |
| 进度解锁系统 | 反序列化后的进度 | 恢复游戏状态 |
| 设置菜单UI | 反序列化后的偏好 | 恢复玩家设置 |
| 音频系统 | 反序列化后的音量 | 恢复音频设置 |

**重要边界**：存档系统是**被动服务**。它不主动知道其他系统的存在——其他系统在数据变化时调用 `SaveManager.save_game()`，存档系统将传入的 `Dictionary` 序列化保存。加载时，存档系统将 `Dictionary` 返回给调用方，由调用方决定如何恢复状态。

---

## Formulas

### 1. 存档大小估算公式 (Save Size Estimation)

The save file size estimation formula is defined as:

`estimated_size_kb = base_overhead + (character_count × character_size) + (song_count × song_size) + (event_count × event_size) + content_size + preference_size`

**Variables:**
| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| base_overhead | base | int | 2-5 | JSON 结构开销（KB） |
| character_count | chars | int | 2-4 | 角色数量 |
| character_size | char_sz | float | 0.5 | 单个角色数据大小（KB） |
| song_count | songs | int | 5-12 | 曲目数量 |
| song_size | song_sz | float | 0.1 | 单首曲目熟练度大小（KB） |
| event_count | events | int | 0-20 | 活跃+历史事件数量 |
| event_size | event_sz | float | 0.3 | 单个事件大小（KB） |
| content_size | content | float | 1-2 | 内容解锁状态大小（KB） |
| preference_size | prefs | float | 0.5-1 | 偏好设置大小（KB） |

**Output Range:** 5 KB 到 50 KB（MVP 规模）
**Example:** 2 角色、5 曲目、5 个事件 → `size = 3 + (2 × 0.5) + (5 × 0.1) + (5 × 0.3) + 1 + 0.5 = 3 + 1 + 0.5 + 1.5 + 1 + 0.5 = 7.5 KB`

**移动端约束**：iOS/Android 应用私有目录通常有 100MB+ 空间，存档大小完全无压力。

### 2. 版本兼容性检查公式 (Version Compatibility)

```
compatibility = check(save_version, game_version)

Where:
- save_version = 存档中的 save_format_version
- game_version = 当前游戏代码支持的最新版本

Returns:
- "compatible" if save_version == game_version
- "migratable" if save_version < game_version AND migration_path_exists
- "too_new" if save_version > game_version
- "unknown" if save_version not recognized
```

**迁移路径检查**：
```
migration_path_exists = all(intermediate_versions have migration_script)
```

即从 save_version 到 game_version 的每一个中间版本都必须有对应的迁移脚本。

### 3. 数据完整性校验公式 (Integrity Check)

```
integrity_score = Σ(field_validity_i) / total_required_fields

Where field_validity = 1 if field exists AND type matches AND value in range
                       0 otherwise
```

**通过阈值**：`integrity_score >= 0.8`（80% 字段有效即可加载，缺失字段用默认值填充）

**Example**: 存档有 20 个必填字段，其中 3 个缺失或无效 → `integrity_score = 17/20 = 0.85` → 允许加载，缺失字段使用默认值。

---

## Edge Cases

- **If 存档文件被外部工具手动修改导致 JSON 格式损坏**：尝试 `JSON.parse()` 失败 → 依次尝试 backup1、backup2、backup3 → 全部失败则进入错误状态，提示玩家新建游戏。
- **If 存档文件存在但为空（0 字节）**：视为损坏，走备份恢复流程。
- **If 设备存储空间不足导致保存失败**：捕获 `FileAccess` 写入错误 → 显示非阻断提示"存储空间不足，请清理后重试" → 保留内存中的游戏状态，下次触发保存时重试。
- **If 玩家在保存过程中强制关闭应用（如设备断电）**：FileAccess 的写入操作是原子性的（先写入临时文件，再重命名），不会留下半写入的损坏存档。下次启动时正常加载。
- **If 存档版本号缺失或 null**：视为版本 0（最早版本），尝试执行从 v0 到 current 的完整迁移链。
- **If 迁移脚本执行过程中出错**：记录错误日志，停止迁移，尝试从备份加载原始版本存档。如果备份也失败，进入错误状态。
- **If 存档中的角色数量与当前游戏版本不匹配**（如旧存档有 2 个角色，新版本支持 4 个）：保留旧角色数据，新角色使用默认初始值。不删除旧数据。
- **If 存档中的曲目 ID 在新版本中不存在**：该曲目熟练度数据保留但标记为"legacy"，不在 UI 中显示。不删除数据（未来可能恢复）。
- **If 玩家频繁切换前后台（1 分钟内 10 次）**：存档频率保护生效——30 秒内只保存一次，最后一次的后台事件触发保存。
- **If 游戏在加载过程中崩溃**：下次启动重新执行完整加载流程。加载是幂等的——多次执行不会导致数据损坏。
- **If 存档完整性校验得分 < 0.8 但 >= 0.5**：允许加载，但显示警告"存档部分损坏，已用默认值修复"。
- **If 存档完整性校验得分 < 0.5**：拒绝加载主存档，尝试备份。备份也 < 0.5 → 进入错误状态。
- **If 应用被卸载后重装**：设备本地存储清空，无存档 → 新建游戏。MVP 不实现云同步。
- **If 玩家在多设备间切换**（如 iPhone + iPad）：各设备独立存档，不共享。MVP 不实现云同步。

---

## Dependencies

**上游依赖**：
| 系统 | 依赖数据 | 必要性 |
|------|----------|--------|
| 无 | — | 存档系统是 Foundation 层服务，无上游硬依赖 |

**下游被依赖**：
| 系统 | 依赖数据 | 数据流向 |
|------|----------|----------|
| 角色属性系统 | 反序列化后的角色状态 | Save/Load → 状态恢复 |
| 曲目数据库 | 反序列化后的熟练度 | Save/Load → 状态恢复 |
| 内容数据库 | 反序列化后的解锁状态 | Save/Load → 状态恢复 |
| 事件模板系统 | 反序列化后的事件实例 | Save/Load → 状态恢复 |
| 时间管理/离线收益 | `last_leave_timestamp` | Save/Load → 离线计算 |
| 关系矩阵系统 | 反序列化后的关系值 | Save/Load → 状态恢复 |
| 声誉系统 | 反序列化后的声誉值 | Save/Load → 状态恢复 |
| 进度解锁系统 | 反序列化后的进度 | Save/Load → 状态恢复 |
| 设置菜单UI | 偏好设置读写接口 | Save/Load ↔ UI |
| 音频系统 | 音量设置读写接口 | Save/Load ↔ Audio |

**软依赖**：
- 云同步服务（MVP 预留接口，不实现）
- 存档加密库（Post-MVP 考虑）

---

## Tuning Knobs

| 参数名 | 当前值 | 安全范围 | 过低效果 | 过高效果 | 关联 |
|--------|--------|----------|----------|----------|------|
| `save_format_version` | 1 | 1+ | 无法定义 | 版本过多增加迁移复杂度 | 兼容性 |
| `auto_save_interval_seconds` | 300 | 60-600 | 保存过于频繁，I/O 开销 | 数据丢失风险增加 | 数据安全 |
| `min_save_interval_seconds` | 30 | 10-60 | I/O 开销大 | 高频操作可能丢失中间状态 | 性能 |
| `max_backup_count` | 3 | 1-5 | 恢复选择少 | 占用存储空间 | 存储 |
| `integrity_threshold_load` | 0.8 | 0.5-1.0 | 过于宽松，加载损坏存档 | 过于严格，正常存档可能被拒 | 稳定性 |
| `integrity_threshold_recover` | 0.5 | 0.3-0.7 | 放弃可修复的存档 | 加载严重损坏的存档，可能崩溃 | 稳定性 |
| `save_file_name` | "save.json" | — | — | — | 文件管理 |
| `l1_core_enabled` | true | true/false | 核心进度不保存（致命） | 无影响 | 功能开关 |
| `l2_prefs_enabled` | true | true/false | 玩家偏好不保存（不便） | 无影响 | 功能开关 |

---

## Visual/Audio Requirements

存档系统本身**不定义视觉资产**，但定义了操作反馈：

| 操作 | 视觉反馈 | 音频反馈 | 时长 |
|------|----------|----------|------|
| 自动保存 | 右上角微型软盘/保存图标，旋转 0.5 秒后淡出 | 无（静默保存） | 0.5s |
| 手动保存 | 中央"已保存"提示，手写风格字体 | 轻柔的吉他拨弦确认音 | 1s |
| 加载中 | 进度条 + 排练室背景图（模糊） | 环境音（排练室的轻微回声） | 1-3s |
| 恢复中 | 进度条 + "尝试恢复..."文字 | 无 | 1-2s |
| 存档错误 | 报错弹窗，手绘风格的警告框 | 走调的吉他弦（不刺耳，但明确） | 直到玩家点击 |

**保存图标设计**：像一枚生锈的回形针别在一张泛黄的纸上——符合"不完美是最高标准"原则。

---

## UI Requirements

存档系统**不直接提供 UI**，但通过接口支持以下 UI：

**设置菜单中的存档管理**（由设置菜单UI实现）：
- "立即保存"按钮
- "最后保存时间"显示
- "存档大小"显示
- "恢复默认设置"按钮（仅重置 L2 偏好，不影响 L1 进度）

**加载界面**（由场景管理器实现）：
- 进度条（0-100%）
- 状态文字（"加载中..." / "校验数据中..." / "尝试恢复..."）
- 错误时的"新建游戏"按钮

**游戏内自动保存提示**（由主界面UI实现）：
- 右上角微型保存图标（仅自动保存时显示 0.5 秒）

---

## Acceptance Criteria

- **GIVEN** 游戏有有效存档，**WHEN** 启动游戏，**THEN** 在 3 秒内完成加载并显示主界面
- **GIVEN** 存档格式版本与当前版本一致，**WHEN** 加载，**THEN** 数据完整恢复，无警告
- **GIVEN** 存档格式版本低于当前版本且迁移路径存在，**WHEN** 加载，**THEN** 执行迁移后成功加载
- **GIVEN** 存档格式版本高于当前版本，**WHEN** 加载，**THEN** 显示"请更新游戏"提示，拒绝加载
- **GIVEN** 存档文件损坏（无效 JSON），**WHEN** 加载，**THEN** 尝试备份恢复，成功后显示"已从备份恢复"
- **GIVEN** 所有备份都损坏，**WHEN** 加载，**THEN** 显示错误弹窗，提供"新建游戏"选项
- **GIVEN** 玩家设置了排练计划，**WHEN** 游戏进入后台，**THEN** 3 秒内完成自动保存
- **GIVEN** 玩家 1 分钟内切换前后台 10 次，**WHEN** 触发保存，**THEN** 只执行 2 次保存（受 30 秒间隔保护）
- **GIVEN** 存档完整性得分 0.85（3/20 字段缺失），**WHEN** 加载，**THEN** 允许加载，缺失字段用默认值填充，显示警告
- **GIVEN** 存档完整性得分 0.3（14/20 字段缺失），**WHEN** 加载，**THEN** 拒绝加载主存档，尝试备份恢复
- **GIVEN** 设备存储空间不足，**WHEN** 触发保存，**THEN** 显示"存储空间不足"提示，不崩溃，保留内存状态
- **GIVEN** 玩家在保存过程中强制关闭应用，**WHEN** 下次启动，**THEN** 存档不损坏（原子写入保证）
- **GIVEN** 存档中存在旧版本的曲目数据（新版本已删除该曲目），**WHEN** 加载，**THEN** 保留数据但不显示，不报错

---

## Open Questions

- **Q1**: MVP 是否需要一个"导出存档"功能（如通过分享菜单发送存档文件）？便于开发者调试，但增加复杂度。
- **Q2**: 云同步接口应该预留到什么程度？是完全不提及，还是定义接口但不实现？
- **Q3**: 存档数据是否需要压缩？JSON 文本在 10-50KB 范围内，压缩收益极小，但 Godot 支持 `FileAccess.COMPRESSION_DEFLATE`。
- **Q4**: 是否需要实现"存档导入"功能用于客服支持？玩家可以将存档发送给开发者排查问题。
- **Q5**: 是否需要定期自动清理旧备份（如保留最近 7 天的备份）？还是只保留固定数量的备份？
